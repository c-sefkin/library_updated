<h1>Welcome to the library App!</h1>
<h2>A place where you can store information about books and authors</h2>

<p><a href="/books">Add a new book</a></p>
<p><a href="/authors">Add a new author</a></p>

<h3>Here are all of the authors in the database:</h3>

<% if @authors.any?() %>
<p>Click on an author to see the information about them and indicate which books they have written:</p>

<ul>
  <% @authors.each do |author| %>
  <li><a href="/authors/<%= author.id() %>"><%= author.name() %></a></li>
  <% end %>
</ul>
<% else %>
  <p>There are no authors added yet.</p>
  <% end %>

  <h3>Here are all the books in this library:</h3>

  <% if @books.any?() %>
  <p> Click on a book to see information about it and indicate the author:</p>

  <ul>
    <% @books.each do |book| %>
    <li><a href="/books/<%= book.id() %>"><%= book.name() %></a></li>
    <% end %>
  </ul>
  <% else %>
  <p>There are no books added yet!</p>
  <% end %>
